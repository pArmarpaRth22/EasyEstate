{
  "name": "final-esayestate---copy",
  "version": "1.0.0",
  "description": "react project full stack",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development node api/app.js",
    "start": "NODE_ENV=production node api/app.js",
    "build": "npm install && npm install --prefix client && npm run build --prefix client"
  },
  "author": "parth",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
